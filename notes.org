* Notes about misc stuff useful
** Subscribe to events
With the following we can subscribe to events of the dialer of OML dialplan:
curl -X POST -u omnileadsami:omnileadsass http://localhost:8888/ari/applications/call_manager_dialer/subscription -H 'content-type: application/json' -d '{"eventSource": "endpoint:PJSIP"}'
TODO: try with "endpoint:PJSIP/omlacd", see if we can config read only the events from dialer trunk
** Maximum number of concurrent calls in FTS production
~ 400
With DB of 1500 contacts
** Avoiding Log exhaustion in Docker
watch -t docker logs --tail 50 foo


| campaign | agents | own_agents | shared agents    |
|----------+--------+------------+------------------|
| c1       |      7 |          2 | (2 . 2), (3 . 3) |
| c2       |      5 |          3 | (2 . 3)          |
| c3       |      3 |          1 | (1 . 3), (1 . 2) |


num_agents_online_assigned_to_it / num_active_campaigns


how to count agents?

agent assigned to n campaigns active : 1/n


sum (1/n_j) for agent i / num_active_campaigns
** Useful queries in OML
INSERT INTO ominicontacto_app_actuacionvigente values (1, True, True, True, True, True, True, True, '00:00', '23:59',4);
** Comand to see active channels in pstn emulator
watch -n1 "docker exec -it oml-pstn-emulator asterisk -rx 'core show channels'"
** Command to see the active channels number in Redis every 1 sec
watch -n1 "docker exec -it oml-redis redis-cli --raw GET 'dialer_pstn_calls'"
** For getting the websockets messages locally
$ docker exec -it oml-app bash
ea499e431cf3:/opt/omnileads/ominicontacto# python manage.py omnidialer_events_listener
